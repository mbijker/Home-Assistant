group:
  home:
    name: home
    view: yes
    entities:
      - group.family
      - group.lights_card
      - group.network_devices
      - group.remote_card
      - group.cctv
      - group.motion
      - group.time